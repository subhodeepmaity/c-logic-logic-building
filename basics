#include <stdio.h> //library header file for standard input & output tasks

int main() //the main function where the execution of programs actually starts
{
    printf("Hello, World!\n");// prints or displays the result that is required
    return 0;//returns value to understand whether the result is true 0r false
}
